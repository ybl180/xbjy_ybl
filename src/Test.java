import com.ybl.sys.entity.Menu;
import com.ybl.sys.service.MenuService;
import com.ybl.sys.service.impl.MenuServiceImpl;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

/**
 * @author ybl
 * @version 1.0.1
 * @conpany 东风标准
 * @date 2019/11/30 08:55
 * @desciption
 */
public class Test {
//    public static void main(String[] args) {
//        MenuServiceImpl service = new MenuServiceImpl();
//        List<Menu> list = service.listAll();
//        List<Menu> first = new ArrayList<>();
//        List<Menu> second = new ArrayList<>();
//
//        first = list.stream().filter(n -> n.getType().equals("1")).collect(Collectors.toList());
//        second = list.stream().filter(n -> n.getType().equals("2")).collect(Collectors.toList());
//
//        System.out.println(first);
//        System.out.println(second);
//    }
}
